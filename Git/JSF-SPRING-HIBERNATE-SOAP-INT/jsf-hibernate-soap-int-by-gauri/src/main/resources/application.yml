server:
  context-path: /employeepage
  port: 9091

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mysql
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
